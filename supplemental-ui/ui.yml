# Publish CNAME to site root so GitHub Pages keeps the custom domain (docs.devcentr.org).
# See https://docs.antora.org/antora/latest/playbook/ui-supplemental-files/#static-files
static_files:
  - CNAME
