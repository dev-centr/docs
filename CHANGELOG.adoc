= Changelog

== 2026-09-25 - Fix injectAssets false for mermaid-client / diagram-lightbox

* Bump `@antora-supplemental/mermaid-client` + `diagram-lightbox` pin to `antora-diagram-engines@f5c293c` so `register({ config })` keeps `length === 1` (and this-binding) and Antora honors playbook `injectAssets: false` (stops Duplicate UI file WARNs against hub `supplemental-ui` mirrors).

== 2026-09-25 - Mermaid client + diagram lightbox packages

* Require `@antora-supplemental/mermaid-client/antora` + `@antora-supplemental/diagram-lightbox` (pinned `antora-diagram-engines@981eb29`) on all hub playbooks; attrs `mermaid-client` / `mermaid-client-mode: client` so Mermaid uses the CDN client path (PlantUML stays on `asciidoctor-kroki`).
* Asciidoctor tree-processor converts `[source,mermaid]` to client hosts before Kroki bake — no dual SVG.
* Head/footer use `{{> mermaid-client-*}}` / `{{> diagram-lightbox-*}}` partials; chrome assets mirrored from the packages into `supplemental-ui` (`injectAssets: false` to avoid double-load). Replaced hub-vendored `site-diagram-zoom.*` with `site-diagram-lightbox.*`.
* Docs: `publishing/antora-diagram-formats.adoc` + `AGENTS.md` (lightbox UX name; Themed SVG still live/unmaintained).


== 2026-09-25 - Home to Tools migration; Agent Rules under Tools

* Added hub `tools-docs` Antora source (`name: tools`, `ROOT:index.adoc`) so `/tools/` is the Tools forest start page (same pattern as Platforms `/platforms/`).
* Removed `agent-rules` from site-nav-tree `include` / `order` in all playbooks; content source remains. Agent Rules docs publish as `tools:agent-rules`.
* Home `nav.adoc`: drop nested Tools children; keep portal `tools/index.adoc` and stub pages for forks/external discovery.
* Portal stub and inventory xrefs point at `tools:agent-rules:*`.

== 2026-09-25 -- Diagram engines doc + zoom/lightbox

* Document Option A (Kroki/Themed SVG bake), Option B (client Mermaid), and hybrid per-diagram how-to on `publishing/antora-diagram-formats.adoc` (borrow-from-light WARN unchanged).
* Vendor chassis `diagram-zoom` into `supplemental-ui` (in-article zoom + pan/zoom lightbox); wire CSS/JS in head/footer.
* Teach `site-mermaid.js` to collect `[.mermaid-client]` / `pre.mermaid` listings so Kroki can keep baking PlantUML while selected Mermaid stays client-rendered.

== 2026-09-25 - Themed SVG dark-text WARN on Antora build

* Added `lib/verify-themed-svg-dark-mode.js` and registered it in the playbooks: at `contentClassified`, WARN when adaptive Themed SVG images still hardcode Mermaid root `#my-svg` fill / `.label` color under `prefers-color-scheme: dark`. Borrow-from-light / parent -> child policy: equal light/dark token values (or `borrowFromLight`) mark stable light islands so nested text is not forced to flip; WARN still fires when a darker adapted surface keeps light-mode text.
* Documented the check on the Antora diagram formats page.

== 2026-09-25 -- Nav icon inheritance, scroll decoupling, footer middot

* SoftNav: same-component nav-tree clicks soft-navigate; content column/window scrolls to top on load while left-rail scroll is preserved (and restored after nav panel swap).
* Vendor `@antora-supplemental/site-nav-tree` 0.4.1 companion JS (`site-nav-tree:scroll-y` sessionStorage) so full cross-component navigations also keep nav scroll.
* Pick up `@antora-supplemental/nav-typology` 0.1.6 so Diataxis How-to icons inherit on cross-component leaves (Bitwarden CLI / gcloud CLI under general-knowledge).
* Footer related-projects separators: replace double-encoded UTF-8 middle dots with `&middot;` (fixes `A·` mojibake).
== 2026-09-25 -- Platforms hub IA + SoftNav expand keys + Source-only lead

* Platforms: shared component start page is `/platforms/` hub; DevCentr + devcentr.org titled nav trees as platform children; removed missing sibling nav paths footgun.
* Home nav: drop untitled Architecture section (pages kept; GK already xrefs them).
* Vendor `@antora-supplemental/site-nav-tree` 0.4.0 companion JS (pathname expand keys + Home/Changelog hard-sort); SoftNav rebind no longer looks like inject/remove.
* Pin `@antora-supplemental/page-context` to `v0.6.7` for shrink-wrapped Source-only lead tables on component homes.

== 2026-09-15 -- page-edit 0.2.4 component-home Source

* Pin `@antora-supplemental/page-edit` to `v0.2.4` so Antora component start pages get the Source | View | Edit row (empty lead shells no longer false-positive on page-context CSS).

== 2026-09-15 -- Drop Home from component breadcrumb trail

* Hub supplemental helper `adt-bc-trail-crumbs` overrides Valentus: with site-nav-tree, strip leading foreign component-root crumbs (especially Home / site home) before the usual current-component-root strip so the underlined trail stays within the current component (house icon still covers site home).

== 2026-09-15 -- Restore breadcrumb trail separators

* Hub `breadcrumbs.hbs` again emits Valentus `/` separator nodes between trail crumbs (via `adt-bc-trail-crumbs`), and keeps the component kicker visible with site-nav-tree (curated picker only) so the mast matches HCI Nerdz / stock Valentus.

== 2026-09-14 -- page-edit 0.2.3 DOCTYPE fix

* Pin `@antora-supplemental/page-edit` to `v0.2.3` so View | Edit is not prepended before the HTML DOCTYPE.

== 2026-09-10 -- page-context 0.6.6 author spacing

* Pin `@antora-supplemental/page-context` to `v0.6.6` so the linked name, circular avatar, and `via \{product}` stay separated in the lead byline and footer author cells.

== 2026-09-10 -- Dark-mode Mermaid diagram box fills

* Bumped `@dev-centr/themed-svg` to **0.2.3** and regenerated themed diagrams / vendor runtime so node fills follow host dark mode (no light boxes under light text).

== 2026-09-10 -- Publish robots.txt with sitemap

* Antora `site.robots` now emits `/robots.txt` with an absolute `Sitemap:` line for `https://docs.devcentr.org`.
